// ============================================================================
// TOOLS
// ----------------------------------------------------------------------------
// Reserved for future custom tool definitions: document generators (docx,
// pptx, xlsx), MCP server configurations (Gmail, Drive, Calendar), and any
// other tool-use definitions beyond Anthropic's built-in web_search.
//
// When a workflow needs document generation or external service access, the
// tool is defined here and referenced by the /api/chat route.
//
// Currently empty — web_search is wired directly in the route handler.
// ============================================================================

export {};
